import { ResetPasswordAuthGuard } from './reset-password-auth.guard';

describe('ResetPasswordAuthGuard', () => {
  it('should be defined', () => {
    expect(new ResetPasswordAuthGuard()).toBeDefined();
  });
});
